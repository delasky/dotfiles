[user]
	name = Daniel Olasky
	email = daniel.olasky@hotschedules.com
[push]
	default = current
[tag]
    sort = version:refname
[alias]
    syncdev = !git checkout dev && git pull && git checkout @{-1} && git rebase dev
[pull]
    rebase = preserve
[merge]
    ff = false
[status]
    showUntrackedFiles = all
[core]
    editor = vim
    excludesfile = ~/.gitignore_global

